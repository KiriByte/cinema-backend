server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://192.168.1.14:5432/cinema_project
    username: postgres
    password: 76sqon9Z
  flyway:
    enabled: true
    locations: classpath:db/migration/auth
    default-schema: auth_service_schema
    schemas: auth_service_schema
  jackson:
    serialization:
      write-dates-as-timestamps: false
    date-format: yyyy-MM-dd HH:mm:ss

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true


jwt:
  secret: myverysuperlongsecretkeyforjwtmyverysuperlongsecretkeyforjwt
  access-token-expiration-minutes: 30    # 30 minutes
  refresh-token-expiration-days: 7 # 7 days